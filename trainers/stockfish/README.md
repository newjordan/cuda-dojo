# Stockfish Binary

This directory is intentionally empty in git.

Install a local Stockfish binary into:

`trainers/stockfish/stockfish_bin`

From the repo root:

```bash
bash ./install_stockfish.sh
```
